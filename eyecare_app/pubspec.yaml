name: eyecare_app
description: "محافظ چشم — یک مداخله‌ی دیجیتال سلامت (Digital Health Intervention) برای کاهش فشار چشمی؛ یادآوری پلک زدن، استراحت کوتاه/طولانی و بررسی فاصله"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <5.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
